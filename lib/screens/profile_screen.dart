import 'package:flutter/material.dart';

class ProfiledScreen extends StatelessWidget {
  const ProfiledScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
